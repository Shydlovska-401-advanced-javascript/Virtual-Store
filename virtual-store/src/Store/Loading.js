

//   export default const loadingReducer = (state = false, action) => {

//     if (action.type === 'LOADING') {
//       return action.payload === true;
//     }
  
//     return state;
//   }
  
//   const loading = (bool) => ({ type: 'LOADING', payload: bool })